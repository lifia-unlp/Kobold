ProvideDefaultOption replaces the default option for the most selected one. This way, most users won't have to change it.

Instance Variables:
	defaultOptionIndex	<Integer>	the option's index that should be set as default

